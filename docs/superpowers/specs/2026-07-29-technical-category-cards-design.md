# 技术资料分类卡片设计

技术资料入口页不再直接列出文章，而是按文章 `category` 聚合为分类卡片。每张卡片显示分类名称与文章数，并链接至该分类的分页文章页；其余内容大板块保持直接文章列表。

新增路由使用 `/category/technical/<category>/[...page]`，仅接受属于技术资料的文章分类。分类页复用现有 `PostPage` 与分页组件；无效分类返回 404。测试覆盖技术入口不渲染文章列表、分类聚合和分类页链接生成。
